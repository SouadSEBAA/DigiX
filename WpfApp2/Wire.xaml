<UserControl x:Class="WpfApp2.Wire"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfApp2"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.ContextMenu >
        <ContextMenu x:Name="menu" Background="Lavender">
            <ListBoxItem Content="Supprimer" MouseUp="Supprimer" />
        </ContextMenu>
    </UserControl.ContextMenu>


    <Grid x:Uid="m" Uid="m" x:Name="m">
        <Path x:Name="outer" StrokeThickness="3.8" Stroke="Black">
        </Path>

        <Path x:Name="wire" StrokeThickness="1.5" Stroke="Black">
        </Path>

    </Grid>
</UserControl>
