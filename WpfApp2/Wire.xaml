<UserControl x:Class="WpfApp2.Wire"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfApp2"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.ContextMenu >
        <ContextMenu x:Name="menu" Background="Lavender">
            <ListBoxItem Content="Supprimer" MouseUp="Supprimer" />
        </ContextMenu>
    </UserControl.ContextMenu>


    

    <Grid>

        <Path x:Name="outer" StrokeThickness="4.65" Stroke="Black" >
            <!--<Path.Resources>
                <Style TargetType="Path" >
                    <Style.Triggers >
                        <Trigger Property="IsMouseOver" Value="true">
                            <Setter Property="Stroke" Value="Blue" />
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </Path.Resources>-->

        </Path>
        <Path x:Name="wire" StrokeThickness="2.55">
        </Path>
    </Grid>
</UserControl>
